# Optional inference runtime dependencies (not used in CI).
# Install only when running real Qwen3-8B inference locally:
#   python3 -m pip install --break-system-packages -r requirements/inference.txt

torch>=2.2.0
transformers>=4.51.0
peft>=0.13.0
accelerate>=1.3.0
